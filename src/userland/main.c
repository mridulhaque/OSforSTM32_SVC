#include<ustdio.h>
#include <kunistd.h> // need to remove
#include <cm4.h>





void main()
{
    kprintf("Inside main started.\n");

    int prv = 0;
    while(1) {
        // unsigned int cur = __getTime();
        // kprintf("Time : %d, passed : %d\n", cur, cur - prv);
        // prv = cur;
        // for(int i = 0; i < 100000000; i++);
    }
    kprintf("Inside main ended.\n");

}