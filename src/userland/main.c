#include<ustdio.h>
#include <kunistd.h> // need to remove
#include <cm4.h>





int main()
{
    kprintf("Inside main started.\n");
    while(1) {
    }
    kprintf("Inside main ended.\n");
    return 0;
}